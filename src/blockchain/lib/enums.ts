export enum TokenTypeEnum {
  UNRARITY,
  LOW,
  MEDIUM,
  HIGH,
}

export enum VoteTypeEnum {
  ABSTAIN,
  POSITIVE,
  NEGATIVE,
}

export enum VotingStatusEnum {
  CREATED,
  ACTIVE,
  INACTIVE,
}
